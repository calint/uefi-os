
    serial_print("pixel format: ");
    switch (gop->Mode->Info->PixelFormat) {
    case PixelBlueGreenRedReserved8BitPerColor:
        serial_print("bgr");
        break;
    case PixelRedGreenBlueReserved8BitPerColor:
        serial_print("rgb");
        break;
    case PixelBitMask:
        serial_print("bit mask");
    case PixelBltOnly:
        serial_print("blt only");
    case PixelFormatMax:
        serial_print("format max");
    }
    serial_print("\r\n");

objdump -S -C -M intel --visualize-jumps=color kernel.o
-d: Disassemble the executable sections.
-C: Demangle the names automatically so you see MyClass::test() instead of the mangled string.
-M intel: (Optional) Use Intel syntax, which is much easier to read for x86_64 kernel development than the default AT&T syntax.
--visualize-jumps=color: (Newer versions) Adds ASCII art lines to help you follow if statements and loops.
